plugins { id 'com.android.application' }
android {
 namespace 'local.certiva.probe'
 compileSdk 35
 defaultConfig { applicationId 'local.certiva.qvacprobe'; minSdk 33; targetSdk 35; versionCode 1; versionName '0.1'; ndk {abiFilters 'arm64-v8a'} }
 compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
 packaging { jniLibs { useLegacyPackaging true } }
}
dependencies { implementation project(':qvac') }
